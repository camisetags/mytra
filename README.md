# mytra
simple crawler to search weather

To run mytra, you must have python >= 3.5.2

Install the dependencies with the following command:

```
    pip install -r requirements.txt
```

To run the project:

```
    scrapy crawl weather
```